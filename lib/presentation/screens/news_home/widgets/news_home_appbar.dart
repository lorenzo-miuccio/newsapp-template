import 'package:flutter/material.dart';

class NewsHomeAppBar extends StatelessWidget {
  const NewsHomeAppBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
